# tmux.conf

```bash
# Clone tmux plugin manager 
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

# Clone tmux config
git clone https://github.com/tontinton/tmux.conf.git ~/.config/tmux

# Enter tmux and download plugins by running <prefix> + I
tmux

```

